import React from 'react';

const Header = () => {
  return (
    <div className='text-xl font-semibold text-gray-900 bg-white p-4 border-b border-gray-300 shadow-sm text-center'>
      React Autocomplete (Combo box) with Arrow Navigation
    </div>
  );
};

export default Header;
